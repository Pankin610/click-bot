package lang.commands;


/**
 * Commands should be implemented as calls to Environment methods with corresponding parameters (as generic style as possible).
 */
public interface Command extends Executable {
}
