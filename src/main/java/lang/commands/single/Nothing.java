package lang.commands.single;

import environments.Environment;
import lang.commands.SingleCommand;

public class Nothing extends SingleCommand {
    @Override
    public void exec(Environment envi){}
}
